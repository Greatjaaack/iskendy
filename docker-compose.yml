services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      # DB_PATH в контейнере всегда указывает в volume (перекрывает .env).
      DB_PATH: /data/iskendy.db
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
