# Искенди — табло заказов

Веб-табло готовности заказов для дюрюмной «Искенди». Касса заносит номер заказа
и двигает его по статусам, а гость с телефона видит, готов ли его номер. Снимает
физическую толпу у окна в пик — гость гуляет и следит за статусом (по QR со
стикера / таблички).

Проект **отдельный** от аналитического дашборда `iskendy_analytic`: другая
аудитория (гость с улицы, а не владелец), другой жизненный цикл, отдельный
деплой.

## Модель

- Статус ведётся **на уровне заказа**. Кассир вписывает номер (с чека), заказ
  получает статус **«готовится»**; когда блюдо готово — переводит на **«готово»**,
  а при выдаче — на **«выдано»** (заказ уходит с табло).
- Три статуса: `preparing` → `ready` → `served`. Можно вернуть заказ на шаг назад
  или удалить ошибочно занесённый.
- Гостевое табло **/board** — две колонки, как в фастфуде: «Готовятся» и
  «Готово ✓». Гость сам находит свой номер. Выданные заказы исчезают.
- QR **один общий** — ведёт на табло `/board`.

Данные ведутся по дню ресторана (часовой пояс из настроек). Заказы за день можно
очистить одной кнопкой («новый день»).

## Стек

- **Backend:** FastAPI + SQLite (stdlib `sqlite3`), JWT-авторизация кассы на
  `hmac` без внешних зависимостей. Одна таблица `orders`.
- **Frontend:** один статичный `index.html` без сборки (палитра «Bakır»,
  Bebas Neue + IBM Plex), опрос статуса раз в 5 сек. Отдаётся тем же бэкендом.
  Тот же файл обслуживает лендинг-мультиссылку (`/`), табло (`/board`) и кассу
  (`/staff`) — экран выбирается по URL.

## Запуск в Docker (рекомендуется)

Один контейнер отдаёт и API, и статичный фронт. БД живёт в volume `app-data`
(состояние переживает пересборку). Нужен заполненный `.env` в корне.

```bash
cp .env.example .env        # задать STAFF_PASSWORD
docker compose up -d --build
```

- Лендинг: http://localhost:8080/
- Гостевое табло: http://localhost:8080/board
- Касса: http://localhost:8080/staff — вход по `STAFF_PASSWORD`.
- Логи: `docker compose logs -f app` · остановить: `docker compose down`.

## Запуск локально без Docker

```bash
cd backend
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp ../.env.example ../.env     # задать STAFF_PASSWORD
STAFF_PASSWORD=<пароль> uvicorn main:app --reload --port 8080
```

## API

| Метод | Путь | Кто | Что |
|-------|------|-----|-----|
| GET  | `/api/status`        | публично | активные заказы (готовятся + готово) |
| POST | `/api/auth/login`    | публично | пароль → JWT |
| POST | `/api/order`         | касса    | занести заказ → «готовится» |
| POST | `/api/order/status`  | касса    | сменить статус (`preparing`/`ready`/`served`) |
| POST | `/api/order/delete`  | касса    | удалить активный заказ |
| GET  | `/api/history`       | касса    | история заказов за день (с метками времени) |
| POST | `/api/day/reset`     | касса    | очистить все заказы за сегодня |
| GET  | `/api/qr?data=<url>` | публично | SVG QR-кода (для печати таблички) |
| GET  | `/api/health`        | публично | проверка живости |

Ответ `/api/status` и всех кассовых ручек: `{ date, orders: [{number, status,
acceptedAt, readyAt, servedAt}], servedCount, updatedAt, now }` — `orders`
содержит только активные (готовятся + готово), отсортированные по номеру.
Метки времени статусов (`acceptedAt` — приём, `readyAt` — готово, `servedAt` —
выдано) хранятся по каждому заказу; полная история дня, включая выданные, —
через `GET /api/history`.

## Дальше (не сделано)

- Автоподстановка номера заказа с кассы (сейчас кассир вбивает номер вручную).
- Опционально: Telegram-бот с push вместо пассивного опроса страницы.
