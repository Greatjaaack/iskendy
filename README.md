# Искенди — статус-борд партий

Веб-табло готовности партий для дюрюмной «Искенди». Гость с талона проверяет,
готова ли его партия и сколько ждать; касса одной кнопкой отмечает готовые
партии. Снимает физическую толпу у окна в пик — гость гуляет и проверяет статус
с телефона (по QR со стикера на упаковке).

Проект **отдельный** от аналитического дашборда `iskendy_analytic`: другая
аудитория (гость с улицы, а не владелец), другой жизненный цикл, отдельный
деплой.

## Модель

- Гриль работает непрерывным тактом. Партия = **7 тушек**, интервал **15 мин**
  (10 готовка + 5 буфер). Рабочее окно 12:00–22:00 → **40 партий/день**,
  **280 позиций/день** (лимит на весь ассортимент, не только балык).
- Статус ведётся **на уровне партии**, а не отдельного заказа: касса жмёт
  «партия готова → +1», статус обновляется у всех гостей этой партии.
- QR **один общий** — ведёт на табло, гость сам вводит номер партии с талона.

Все цифры (партий в день, старт, интервал, размер партии) — редактируемые
параметры, не хардкод.

## Стек

- **Backend:** FastAPI + SQLite (stdlib `sqlite3`), JWT-авторизация кассы на
  `hmac` без внешних зависимостей.
- **Frontend:** один статичный `index.html` без сборки (палитра «Bakır»,
  Bebas Neue + IBM Plex), опрос статуса раз в 5 сек. Отдаётся тем же бэкендом.

## Запуск в Docker (рекомендуется)

Один контейнер отдаёт и API, и статичный фронт. БД живёт в volume `app-data`
(состояние переживает пересборку). Нужен заполненный `.env` в корне.

```bash
cp .env.example .env        # задать STAFF_PASSWORD
docker compose up -d --build
```

- Гостевое табло: http://localhost:8080/
- «Экран персонала» — ссылка внизу табло, вход по `STAFF_PASSWORD`.
- Логи: `docker compose logs -f app` · остановить: `docker compose down`.

## Запуск локально без Docker

```bash
cd backend
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp ../.env.example ../.env     # задать STAFF_PASSWORD
STAFF_PASSWORD=<пароль> uvicorn main:app --reload --port 8080
```

## API

| Метод | Путь | Кто | Что |
|-------|------|-----|-----|
| GET  | `/api/status`       | публично | состояние дня для табло |
| POST | `/api/auth/login`   | публично | пароль → JWT |
| POST | `/api/batch/ready`  | касса    | +1 к готовым партиям |
| POST | `/api/batch/undo`   | касса    | −1 к готовым партиям |
| POST | `/api/day/reset`    | касса    | обнулить готовые сегодня |
| PUT  | `/api/settings`     | касса    | всего партий / старт / интервал |
| GET  | `/api/health`       | публично | проверка живости |

## Дальше (не сделано)

- Деплой на `iskendy.ru` + редирект `linku.su/iskendy → iskendy.ru`
  (стикеры уже напечатаны на старый URL).
- Автоподстановка номера партии на чек/талон с кассы (сейчас — вручную по
  бумажному листу партий).
- Опционально: Telegram-бот с push вместо пассивного опроса страницы.
